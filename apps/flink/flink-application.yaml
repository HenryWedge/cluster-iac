apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: flink-cluster
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/HenryWedge/cluster-iac
    targetRevision: HEAD
    path: apps/flink/resource
  destination:
    server: https://kubernetes.default.svc
    namespace: flink
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true